export interface authTypes {
  nome: string;
  email: string;
  senha: string;
  cpf: string;
  telefone: string;
}

export interface loginType {
  email: string;
  senha: string;
}

export interface myPerfil {
  nome: string;
  telefone: string;
  foto_perfil: string;
}
